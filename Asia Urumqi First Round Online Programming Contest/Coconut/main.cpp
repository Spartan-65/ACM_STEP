#include <bits/stdc++.h>
using namespace std;
const int maxn=10000;
int c[maxn];
int main()
{
    int t;
    int n,b;
    while(~scanf("%d",&t))
    {
        while(t--)
        {
            scanf("%d%d",&n,&b);

        }
    }
}

#include <bits/stdc++.h>
using namespace std;
const int maxn=160;
char a[maxn][maxn];
int T,R,C;
int main()
{
    while(~scanf("%d",&T))
    {
        while(T--)
        {
            scanf("%d%d",&R,&C);
            for(int i=0;i<R;i++){
                scanf("%s".a[i]);
            }


        }
    }
    return 0;
}

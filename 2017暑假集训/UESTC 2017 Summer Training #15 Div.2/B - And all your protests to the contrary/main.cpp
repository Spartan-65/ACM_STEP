#include <bits/stdc++.h>
using namespace std;
int main()
{
    int P;scanf("%d",&P);
    while(P--)
    {
        int id;scanf("%d",&id);printf("%d ",id);
        int n;scanf("%d",&n);
        int ans=0;
        if(n%3==0) ans++;

    }
    return 0;
}

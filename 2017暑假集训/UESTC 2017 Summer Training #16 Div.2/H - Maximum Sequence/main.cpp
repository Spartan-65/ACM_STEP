#include <bits/stdc++.h>
using namespace std;
const int maxn=250005;
int a[maxn],b[maxn];
int n;
int main()
{
    while(~scanf("%d",&n))
    {
        for(int i=0;i<n;i++)scanf("%d",&a[i]);
        for(int i=0;i<n;i++)scanf("%d",&b[i]);
    }
    return 0;
}

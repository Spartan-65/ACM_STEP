#include <bits/stdc++.h>
using namespace std;
cosnt int maxn=7*1e4;
int a[maxn];
int main()
{
    int T;scanf("%d",&T);
    while(T--)
    {
        int p=0;
        while(1)
        {
            int t;scanf("%d",&t);
            if(t==0)break;
            a[p++]=t;
        }

    }

    return 0;
}

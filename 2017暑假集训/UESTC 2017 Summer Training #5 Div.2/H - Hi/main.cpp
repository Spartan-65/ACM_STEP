#include <bits/stdc++.h>
using namespace std;
const int maxn=55;
long long a[maxn];
int main()
{
    int n;
    while(~scanf("%d",&n))
    {
        for(int i=0;i<n;i++)
            scanf("%lld",&a[i]);
    }
    return 0;
}
